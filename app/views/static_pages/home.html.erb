<div class="center jumbotron">
	
	<h1>Welcome</h1>
	<h2>This is the home page.</h2>
	

	<% if !user_signed_in? && !admin_signed_in? %>
		<%= link_to "Sign up now!", new_user_registration_path, class: "btn btn-lg btn-primary" %>
	<% end %>

</div>
<%if false %>
<div>
	<% if admin_signed_in? %>
		<%= render 'reported_comments' %>
	<% end %>
</div>
<% end %>